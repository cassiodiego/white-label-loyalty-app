name: white-label-loyalty-app
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Build
      run: swift build
    - name: Run tests
      run: swift test
